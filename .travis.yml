language: php
php:
  - "5.3"
  - "5.4"

before_install:
  - pecl -q install gearman && echo "extension=gearman.so" >> `php --ini | grep "Loaded Configuration" | sed -e "s|.*:\s*||"`
  - phpenv rehash

script:
  - cd tests/
  - phpunit
