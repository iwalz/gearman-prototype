language: php
php:
  - "5.3"
  - "5.4"

before_install:
  - printf "\n" | sudo pecl -q install gearman 
  - echo "extension=gearman.so" >> ~/.phpenv/versions/$(phpenv version-name)/etc/php.ini
  - phpenv rehash
  - php server.php &

script:
  - cd tests/
  - phpunit
